import { HttpBookDirective } from './http-book.directive';

describe('HttpBookDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpBookDirective();
    expect(directive).toBeTruthy();
  });
});
